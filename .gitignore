# Python / dev environment
.venv/
uv.lock
__pycache__/
*.pyc
logs/*.out
logs/*.err

# Local scaffold (not part of workshop)
CLAUDE.md
CHANGELOG.md
pyproject.toml
configs/
data/
docs/
scripts/
src/
tests/
.claude/
build_deck.py

# Only track the latest pptx version in git
# (old versions stay local, add new version explicitly with git add)
2026-Git-2.1.pptx
